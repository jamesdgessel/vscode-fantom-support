{
  "name": "fantom-support-server",
  "private": true,
  "version": "0.0.1",
  "main": "../out/server/server.js",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "lint": "eslint src",
    "test": "mocha --require ts-node/register src/**/*.test.ts",
    "postCompile": "npm run copy-non-ts",
    "copy-non-ts": "copyfiles -u 2 \"src/**/*.!(ts)\" out/server"
  }
}
