{
  "name": "fantom-support-server",
  "private": true,
  "version": "0.0.1",
  "main": "../out/server/server.js",
  "scripts": {
    "compile": "node ../esbuild.config.mjs --server",
    "watch": "node ../esbuild.config.mjs --server --watch",
    "lint": "eslint src",
    "test": "mocha --require ts-node/register src/**/*.test.ts"
  },
  "dependencies": {
    "vscode-languageserver": "^8.1.0",
    "vscode-languageserver-textdocument": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "20.x"
  }
}
